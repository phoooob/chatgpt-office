export enum Code {
    SUCCESS = 0,
    FAIL = -1
}

export enum Roles {
    USER = 'user',
    ASSISTANT = 'assistant',
    SYSTEM = 'system'
}

export enum Models {
    GPT3 = 'gpt-3.5-turbo',
    GPT4 = 'gpt-4'
}
